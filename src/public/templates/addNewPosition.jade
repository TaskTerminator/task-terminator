h1.text-center
  | Add New Position Form 
div.employeeAddForm
  form
    .form-group
      label(for="PositionName")
        | Position Name:  
      input.form-control(type="text" ng-model="newPosition.name" id="PositionName")
    .form-group
        label(for="DepartmentsField")
          | Department: 
        select.form-control(ng-model="newPosition.department" id="EmployeeDepartmentField" ng-options="department._id as department.name for department in company.departments")
    .list-group.employeeList
      h3 Employees:
      a.list-group-item(ng-repeat='employee in employees')
        .row
          .col-xs-4
            | {{employee.identification.name.fullName}}
    button.btn.btn-primary.btn-lg(ng-click="addPosition(newPosition)") Submit